experiment:
  name: benchmark_v1
  description: Current exploratory full-sample benchmark defaults.
  hypothesis: Baseline settings provide the reference for all exploratory full-sample experiments.
  changed_variable: none
  baseline_experiment: benchmark_v1
  expected_result: Establish stable benchmark metrics and diagnostic artifacts.
  success_criteria: MCTS is at least a WEAK PASS versus Regime Rule SPY/Cash.

mcts:
  sampler: "path"
  use_full_action_grid: false
  prior_band: 0.5
  drawdown_penalty: 0.1
  opportunity_cost_weight: 0.2
  action_inertia_threshold: 0.0005
  multi_asset_hold_rollout_prob: 0.95
  multi_asset_action_inertia_threshold: 0.001
